# Copy this file to .env.local and fill in real values.
# Never commit real API keys.

# Supabase (required for most app features)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI Features (at least one required for AI chat/search)
# Recommended: Vercel AI Gateway (Lovable-style)
# Get the key from Vercel → (your project) → Storage/AI Gateway → API Keys (or your team settings).
AI_ASSISTANT_ENABLED=false
AI_GATEWAY_API_KEY=

# Optional: override the default Gateway models
# Examples:
# - openai/gpt-4o-mini
# - anthropic/claude-haiku-4
# - google/gemini-2.0-flash
AI_CHAT_MODEL=openai/gpt-4o-mini
AI_VISION_MODEL=google/gemini-2.0-flash

# Optional: feature-specific models (fallback to AI_CHAT_MODEL / AI_VISION_MODEL)
AI_SEARCH_MODEL=
AI_LISTING_MODEL=

# Fallback (if you don't use Gateway): set ONE of these
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
GROQ_API_KEY=

# Optional: control direct-provider selection (useful in dev)
# Example: force OpenAI first then Groq:
# AI_DIRECT_PROVIDER_ORDER=openai,groq
# Or disable Gemini entirely if your key has quota=0:
# AI_DISABLE_GOOGLE=true
AI_DIRECT_PROVIDER_ORDER=
AI_DISABLE_GOOGLE=false
AI_DISABLE_OPENAI=false
AI_DISABLE_GROQ=false

# shadcnblocks (optional)
# Required only if you want to install paid blocks, e.g.
#   pnpm dlx shadcn add @shadcnblocks/product-detail6
# Get a key at https://shadcnblocks.com/dashboard/api
SHADCNBLOCKS_API_KEY=
