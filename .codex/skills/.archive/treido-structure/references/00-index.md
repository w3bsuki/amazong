# treido-structure references

SSOT + rails:

- `AGENTS.md` (root invariants + folder rules list)
- `docs/AGENTS.md` (rails + routing)
- `docs/WORKFLOW.md` (phases + contracts)
- `docs/03-ARCHITECTURE.md` (boundaries + caching)
- `docs/04-DESIGN.md` (Tailwind v4 tokens; forbidden patterns)
- `.codex/stack.yml` (stack facts + conventions)

Common verification:

- `pnpm -s typecheck`
- `pnpm -s lint`
- `pnpm -s styles:gate`
- `pnpm -s knip`
- `pnpm -s dupes`

