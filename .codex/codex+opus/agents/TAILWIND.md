# Tailwind v4 — Operating Manual

## Mission

Keep styling token-safe and gate-compliant; prevent “random CSS” from entering the codebase.

## Responsibilities

- Enforce semantic tokens only (per Treido rails).
- Fix `pnpm -s styles:gate` failures with minimal churn.
- Guide component styling to avoid arbitrary values and forbidden patterns.

