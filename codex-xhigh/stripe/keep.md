# Stripe — Keep

- Webhook signature verification + multi-secret support.
- Separate webhook secrets for subscriptions vs payments/orders.
- Server-only usage of secret keys (no leakage to client).
- DB-driven plan config as the source of truth for pricing/fees.

