# UI Lane Audit (Tailwind v4 + shadcn) — 2026-01-20

## Evidence (commands + outputs)

- `pnpm -s styles:scan` → `codex-xhigh/ui/styles-scan-2026-01-20.log`
  - Totals: `palette=244`, `gradient=0`, `rawGradient=0`, `arbitrary=166`, `hex=37`
  - Full reports: `cleanup/palette-scan-report.txt`, `cleanup/arbitrary-scan-report.txt`
- Hardcoded hex scan → `codex-xhigh/ui/hardcoded-hex-2026-01-20.txt` (37 matches)
- Hardcoded color fn scan (`rgb/rgba/hsl/hsla`) → `codex-xhigh/ui/hardcoded-color-fns-2026-01-20.txt` (50 matches)
- shadcn boundary checks (0 matches)
  - `components/ui` importing `@/app` → `codex-xhigh/ui/components-ui-app-imports-2026-01-20.txt`
  - `components/ui` importing `@/hooks` → `codex-xhigh/ui/components-ui-hooks-imports-2026-01-20.txt`

## Critical (blocks execution)

- [ ] Arbitrary values still very high (166) → `codex-xhigh/ui/styles-scan-2026-01-20.log` → treat demo pages as prime deletion targets once confirmed unused (Knip already flags many).

## High (do next)

- [ ] Hardcoded color usage in code/CSS (hex + rgba/hsl) → `codex-xhigh/ui/hardcoded-hex-2026-01-20.txt` + `codex-xhigh/ui/hardcoded-color-fns-2026-01-20.txt` → replace with design tokens or delete unused/demo surfaces.
- [ ] Palette scan offenders (244) → `cleanup/palette-scan-report.txt` → most offenders appear concentrated in `/demo/*` and sell flow steps; needs triage (delete vs refactor to tokens).

