# Stripe — Remove / Avoid

- Duplicate processing of the same Stripe event across multiple endpoints.
- Trusting `Origin` headers for redirects or safety checks.
- Hardcoded fee percentages in code (use plan config).

