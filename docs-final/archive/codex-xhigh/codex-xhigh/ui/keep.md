# UI — Keep

- shadcn primitives in `components/ui/*` only.
- Token system in `app/globals.css` (single source of truth).
- The existing audit baselines in `cleanup/*` and the UI sprint plan in `TODO.md`.

